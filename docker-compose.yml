version: '3'

services:
    unblockneteasemusic:
        build: .
        env_file: .env
        environment:
            NODE_ENV: production

        # Change sources here
        command: ["-o", "kuwo", "kugou", "bodian", "pyncmd", "bilibili", "bilivideo"]

        ports:
            - ${HTTP_PROXY_PORT:-58080}:8080
            - ${HTTPS_PROXY_PORT:-58081}:8081